interface:
  display_name: "Deck Web Rebuilder"
  short_description: "Viewport-bounded bilingual slide-to-web rebuilder."
  default_prompt: "Use $deck-web-rebuilder to fully understand this deck, preserve its slide logic, and produce a faithful interactive bilingual web presentation with strict slide boundaries."
